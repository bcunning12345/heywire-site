<section class="section section--cta">
  <div class="container cta">
    <div>
      <h2 class="h2 cta__title">{{ ctaTitle or "Need a wire pulled?" }}</h2>
      <p class="cta__text">{{ ctaText or "Get a real price in about a minute, then pick the window that works for your property." }}</p>
    </div>
    <a class="btn btn--primary btn--large" href="/say-hey/">Just say HEY {% include "svg/arrow.svg" %}</a>
  </div>
</section>
